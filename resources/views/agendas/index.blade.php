@extends('posts.postmaster')

@section('content')
    {{--@if(count($agenda->users))
        <ul class="list-group">
        @foreach($agenda->users as $user)
            <div class="card">
                {{ $user->email }}
            </div>
        @endforeach
        </ul>    
    @endif--}}
@endsection